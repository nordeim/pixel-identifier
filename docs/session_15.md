Workspace refreshed — `git pull` confirmed main @ `e77bde8` (the Round-17 ship). Core docs re-verified (PAD v1.16, the R17 fact blocks); baseline gate GREEN: lint ✓, typecheck ✓, 484 tests / 51 files, build ✓ — the codebase matched the documented R17 state exactly. The scandihaven skills catalog was reviewed for tooling (agent-browser confirmed available; verification-and-review-protocol consulted for the evidence-gate discipline).

Round-18 was session_14's suggested drift watch — executed with a stricter methodology: besides the R17 order-sensitive tokenizer diff of all 7 dashboard pages, this round added (1) a full class-string inventory diff of the marketing landing (token-set-equal/order-different strings isolated from reveal/feed animation state) and (2) **geometry probes** — getBoundingClientRect + computed styles on the settings Profile card and every auth-form field group — the first round to measure rendered geometry rather than DOM strings.

**The live stayed 100% stable since R17** (0 class/tag changes on all 7 dashboard pages — the third consecutive stable audit). Every remaining live-vs-local dashboard diff was traced to a documented residual (D2 form machinery, D3/D4 aria, state-dependent install notice) or tokenizer alignment noise (proven by direct byte-extraction of the activity rows).

The stricter probes found what string diffs could never see. **A1 — the settings Profile card rendered broken geometry**: the classless server-action `<form>` was the only child of the `space-y-4` card body, and TW4's space-y only spaces DIRECT children — the field groups rendered flush (0px gaps vs the live's 16px) and the Save button sat 72px high in a clone-authored `flex items-center gap-3` wrapper; the disabled email input also lacked the live's `opacity-60`. **A2 — every auth form's label→input gap was 8px short**: TW4 compiles `space-y-*` as margin-block-end on the PRECEDING sibling, and vertical margins on inline `<label>`s are ignored by CSS layout — measured 3px vs the live's 11px on login/signup (computed-style probe showed the 8px margin set but ignored). **A3** — the install platform Card carried an inert clone-authored `aria-labelledby`.

The marketing inventory diff then found the bundle was never byte-verified at the atom level: the R10–R12 pins covered CTAs/kickers/card chrome, and the R15–R17 "marketing stable" checks compared text-level section lengths (a class rebuild is invisible to them). ~20 order-drift strings plus real drifts: pricing subtitle (missing `max-w-md mx-auto`, `•` vs the live's `·`, no always-on spacer line), comparison win checks (`text-green-600` vs the live's teal `text-accent`, extra font-medium, span pill), benefits/how-it-works/CTA/announcement chips (`gradient-hero-light` vs the live's full `gradient-hero`), feed avatars (span+gradient class vs the live's div+inline solid background), hero CTAs (flat anchors vs the live's anchor-around-button with different tails), header chrome (`max-w-7xl` vs the live's `container` utility, span wordmark vs the lockup on the anchor, `gap-8` vs `gap-7`), footer wordmark (extra tracking-tight), the benefits grid (`gap-12` vs `gap-14`), stat labels (semibold vs font-medium), and the B2B icon missing the live's lucide double-name.

Plan written (`docs/plans/2026-09-19-round18-marketing-realignment.md`), validated against every source site, then **TDD: 30 RED pins → GREEN** (25 in the new `tests/marketing-r18-parity.test.tsx`, 5 in the content-parity R18 blocks) + 4 superseded R10-era pins updated. The app-bundle fixes: the settings form carries `space-y-4` with the Save button in direct flow, `opacity-60` on the email input, a scoped `globals.css` rule restoring v3 space-y semantics for `[label, input]` groups (no pinned class string touched), and the inert aria-labelledby dropped. The marketing realignment touched 12 components (pricing, features, how-it-works, live-feed, hero, site-header, wordmark fragment, faq-footer, announcement-bar, social-proof, plus the compare/pricing CTA structures).

**Full gate GREEN: 519 tests / 52 files** (+35). Browser verification: dashboard re-diff shows only documented residuals; the landing class-order inventory shows **zero order-drifts**; the button inventory is **zero diffs (16 = 16)**; the settings Profile card now matches the live **pixel-exactly** (groups 237/325/413, inputs 269/357/445, Save 501); auth label→input gaps 11px on both sides. E2E smoke green: pricing toggle annual→monthly, plan-intent CTA hrefs, settings render with the opacity-60 tail. New ruling D5 documents the kept invisible functional chrome (switch semantics, section landmarks, focus-brand rings, reveal machinery, feed-row hook, decorative aria-hidden).

Docs updated: PAD v1.17, README (519 + R18 bullet), AGENTS.md (the TW4 space-y seam + marketing emission conventions + D5), CLAUDE.md (geometry-parity principle), plan execution log, evidence README, worklog, session_15 (this log). Ship: atomic commits on main + push via the SSH wrapper.

## What this round delivered

**Round-18 drift watch → geometry verification + marketing class realignment.** The live is stable across three consecutive audits (R16→R17→R18 — zero content-layer changes). The round's methodology upgrade (geometry probes) closed the last class of invisible drift: layout bugs that class-string pins cannot see. The settings Profile card and every auth form now match the live's rendered geometry exactly; the marketing bundle's class emission (orders, gradients, CTA structures, wordmark asset, header chrome) is pinned to the live's current build.

**Remediation (TDD, 30 RED → GREEN + 4 superseded pins, suite 484/51 → 519/52):**
- **Settings Profile card**: form `space-y-4` + direct-flow Save button + `opacity-60` email input — geometry now pixel-exact
- **Auth forms**: the `.space-y-2 > label + input` CSS seam restores the live's 11px label→input gaps (login/signup/forgot)
- **Marketing realignment**: 12 components realigned (subtitle/separators/spacer line, text-accent checks, full gradient-hero chips, div avatars with inline backgrounds, anchor-wrapped CTAs, PNG wordmark, container header, gap-14 grid, ~20 order strings, data-URI zap, double-name building icon)
- **Ruling D5**: invisible functional a11y chrome kept + documented; inert chrome (platform Card aria-labelledby) removed

**Verified**: tokenizer re-diff (documented residuals only), class-order/button inventories (zero diffs), geometry re-probe (pixel-exact settings, 11px auth gaps), E2E smoke (toggle, plan-intent hrefs, settings render). Shipped as atomic commits on main, push verified via `docs/ssh_git_wrapper_v3.py`, key shredded post-push.

**Suggested next steps**: the live is stable across three audits — Round-19 can re-verify with the R18 toolchain (tokenizer + class-inventory + geometry probes) and extend the geometry probes to the marketing bundle (hero/feed/section geometry — this round pinned strings and verified app-bundle geometry); the remaining documented residuals (aria-labels, aria-controls, reveal-machinery representation) stay as D5-class unless byte-exact attributes become a goal; functional parity could deepen next (e.g., the domains page's live validation states, the visitors detail sheet's interactive states).
