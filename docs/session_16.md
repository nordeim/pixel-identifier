Workspace refreshed — the repo sat at main @ `7c5c0bb` (the Round-18 ship), clean tree. The R18 state re-verified (PAD v1.17 fact blocks, 519/52 baseline gate GREEN: lint ✓, typecheck ✓, build ✓ — the codebase matched the documented R18 state exactly). This session continued the interrupted Round-19 audit: the capture phase (dashboard pages + landing, live + local, plus the new marketing geometry probes) had completed and surfaced two real findings; this session reconstructed the evidence (`/tmp/r19-caps` survived), re-extracted the live's definitive sources, and drove the round to remediation and ship.

**The live stayed 100% stable since R18** — the **fourth consecutive stable audit**: the marketing body DOM byte-identical (86,004 chars, md5 d2550c77…), zero token drift on all 7 dashboard pages, the dashboard clone-side at its documented-residual state (D2/D4 only). Auth geometry regression probes held R18's 11px label→input gaps on both sides.

**F1 — every marketing `.gradient-hero` rendered DARK.** The geometry probes measured the how-it-works step chips at `linear-gradient(135deg, rgb(255,170,0) 0%, rgb(255,217,26) 50%, rgb(245,143,0) 100%)` on the live vs the clone's dark `rgb(15,17,26)→rgb(43,35,18)` — and the same divergence on the announcement bar and CTA banner. Root cause traced to the live's two-bundle architecture: the marketing bundle's CSS defines ONE `.gradient-hero{background:var(--gradient-hero)}` with the amber 3-stop on `:root` (not redefined in `.dark`), while the clone's single definition was the auth/app-bundle dark sweep. R18 had pinned the class STRINGS (every marketing surface emits `.gradient-hero`) but never the CSS RESOLUTION — the exact blind spot of string-level pins. Fix: `.marketing-scope .gradient-hero` carries the amber image (byte-exact hex of the live's hsl stops); the auth canvas sits outside the scope and keeps the dark sweep. The dead `.gradient-hero-light` utility retired (zero references; the live's marketing bundle defines no such class). VLM-verified: amber chips, amber CTA banner, dark navy-to-brown login canvas.

**F2 — the hero "Live Visitor Feed" widget's row model was wrong end-to-end.** Extracted the live's implementation from its bundle (symbols PD/TD/ND in index-C3AAh5Je.js): a 5-entry roster {label, email, delay} with staggered delays [0, 1.8, 3.6, 5.4, 7.2]s and varied labels ("Anonymous Visitor"/"Unknown User"/"Site Visitor"), a per-row phase machine enter→scan (delay·1000+600ms)→reveal (+1600)→done (+3200, row unmounts), a 10s parent cycle re-mounting the whole roster, rows at `top = index*56+12`, the avatar animating backgroundColor muted→primary at reveal with a User→Mail icon swap, a "Matching…" opacity-pulse badge during scan and a ✓ badge during reveal. The clone shipped 8 static entries (including emails the live never renders — sofia.larsen@yahoo.com, d.chen@brightpathlabs.com), a 2.6s offset rotation, static identified/anonymous statuses, and `top = 8+slot*56`. Rewrote `live-feed.tsx` to the live's model (CSS keyframes replace framer-motion; the motion inlines stay D5-class reveal machinery). Post-fix the rows measure **offsetTops 12/68/124/180/236 — identical to the live probe**; the VLM widget-crop comparison found layout, stats strip (847/169), colors and structure identical (row text differs only by rotation phase at capture).

**F3 — ruled a non-finding.** The live's avatars 2-5 returned HTTP 404 during the capture session; all five return 200 on re-check (transient CDN hiccup — the R8-era 404 guard pins the local set). Ruling: never replicate a live-side defect.

**F4 — the live swapped its hero trust-row avatar photos.** The live's current set (avatar-1-xs9X7jqQ…avatar-5-D6miPMAM, 512px, content-hashed names) are different people from the clone's R8-era 96px set — VLM-verified all five columns differ. Replaced with the live's current photos downscaled to the repo's 96px convention (displayed at 32px, 3× DPR coverage), guarded by an offline perceptual pin (8×8 average-luma threshold hashes captured from the live, ≤24-bit tolerance).

Plan written (`docs/plans/2026-09-19-round19-geometry-audit.md`), validated against every source site, then **TDD: 12 RED pins → GREEN** (new `tests/marketing-r19-parity.test.tsx`) + 2 superseded R18 B5 pins updated to the phase model (the static render is the t=0 enter state; reveal-state strings pinned at source level — the old pins had asserted the pre-phase static render with emails baked in).

**Full gate GREEN: 531 tests / 53 files** (+12). Browser verification: **F1 PASS** (chips ×4 amber, announcement bar amber, CTA banner amber, login dark — computed styles), **F2 PASS** (feed tops identical), **F4 PASS** (5 avatars loaded), **settings geometry PIXEL-EXACT** (groups 237/325/413, Save 501 on both sides — the live's settings page has no `<form>` element, D2 machinery, so the regression probe now uses the form-independent card selector; the live login needed one retry after a transient reject). E2E console-error sweep: **zero errors** across 10 pages. Screenshots captured to `docs/screenshots/` (7 files: hero+feed, chips, CTA banner, login canvas, dashboard, settings, live landing reference). One operational hiccup: the first verification run unknowingly probed a **zombie server** from the interrupted session holding port 3000 (EADDRINUSE + stale assets — the source of momentarily empty probes); killed it, re-copied standalone static+public, re-ran clean.

Docs updated: PAD v1.18, session_16 (this log), plan + execution log, AGENTS.md (R19 facts: the two-resolution gradient seam + feed phase model), CLAUDE.md (computed-style parity principle), README (531 + R19 bullet), worklog. `.env.example` re-verified matching (DATABASE_URL / NEXTAUTH_SECRET / NEXTAUTH_URL; tracked since e95a20e). Ship: atomic commits on main + push via the SSH wrapper with the operator key (fingerprint verified, key shredded post-push).

## What this round delivered

**Round-19 — the marketing computed-style audit.** The live is stable across four consecutive audits (R16→R19). The round extended R18's geometry probes from the app bundle to the MARKETING bundle's computed styles and closed the class of drift that neither DOM-string pins (R10–R18) nor app-bundle geometry (R18) could see: CSS RESOLUTION and RUNTIME MODELS.

**Remediation (TDD, 12 RED → GREEN + 2 superseded pins, suite 519/52 → 531/53):**
- **F1 scoped amber gradient-hero**: one bundle, two resolutions — `.marketing-scope .gradient-hero` (amber 3-stop) + bare `.gradient-hero` (auth dark sweep), mirroring the live's marketing/app bundle split; dead `.gradient-hero-light` retired
- **F2 live feed phase machine**: 5-entry roster with the live's labels/emails/stagger, enter→scan→reveal→done timing constants, 10s cycle re-mount, `index*56+12` row tops, avatar muted→primary transition, Matching…/✓ phase badges
- **F4 avatar photos**: the live's current set at the repo's 96px convention + offline perceptual hash pin
- **F3 ruling**: transient live CDN 404s are a non-finding — never replicate a live-side defect

**Verified**: computed-style probes (amber/dark split), feed tops identical to the live, settings pixel-exact (form-independent selector — the live has no form), zero console errors on 10 pages, VLM visual confirmations, 7 screenshots in `docs/screenshots/`.

**Suggested next steps**: the live is stable across four audits — Round-20 should re-run the full R19 toolchain (tokenizer + class-inventory + computed-style + geometry probes) as a drift watch; the remaining known divergences are the documented residuals (D2 server-action form machinery, D4 lucide path precision, D5 reveal-machinery representation — framer-motion inline styles on the live's feed/stat elements); functional parity could deepen next (the pricing toggle's live URL-state sync, the visitors table's live sort states, or the domains page's validation flows). The repo's audit toolchain now spans four generations of probes (DOM strings → order-sensitive tokens → geometry → computed styles) and is ready for a fifth (runtime state observation over time — sampling the live's rotation phases directly).
