R24 drift watch — full session log. The workspace had been reset, so the round restarted from a fresh clone (remote still at `2806f50`; the interrupted session's uncommitted work was lost and rebuilt from its preserved narrative).
Workspace rebuilt: clone, npm install, .env (`file:../db/custom.db`), db:push + db:seed, paramiko venv + ssh shim + deploy key (fingerprint matches the R23 record), workspace worklog re-created.
Core docs re-read (AGENTS/CLAUDE/README/PAD v1.22/SKILL) + session_20/21 + worklog; the R23 gates re-confirmed at base.
Dev server up (`db: "up"` — the R23 seam holds). agent-browser loaded for the live probes.
Logged into the live app (probe account). The R24 named targets probed first:
- Settings save flow: the live's Save button renders byte-matching the R18 pin; its flow is PATCH 204 + refetch, SILENT (no toast). Only the pending spinner diverged (live: LoaderCircle `h-3.5 w-3.5 mr-1.5` ALONGSIDE "Save Changes"; clone: h-4 w-4 REPLACING the label).
- The live's Danger-Zone Delete is DEAD (no onClick, no dialog) — the clone's typed-confirm flow stays a value-add.
- Visitors row click: the live's rows are INERT (no sheet, no handler) — the clone's detail Sheet stays a value-add. No drift.
- Bundle hashes: app `index-nhmKaUsm.js` = R21's record, marketing `C3AAh5Je.js` = R20's — no redeploy (R19's `n3AAh5Je` was already stale). The opening "hash drift" alarm was a false positive.
- Prisma 6.19.3 both sides; `db:push` in-sync against `<repo>/db/custom.db`.
The full-surface sweep (dashboard, visitors, settings, install, 404, marketing desktop+mobile) then found the round's real drift:
- The live's dashboard KPI "New This Week" carries a trend badge the clone lacks — decoded from the bundle: `((c-d)/d*100).toFixed(1)`, explicit `+` at >=0, EMPTY when last week = 0; `up` picks text-neon-green + TrendingUp vs text-destructive + TrendingDown (icon `h-3 w-3 mr-0.5`).
- Top Pages renders "1 views" — the live NEVER singularizes.
- The bell button's classes end `h-10 w-10 relative` (live source: size="icon" + className="relative").
- The Export button is a `<button>` (client-side CSV in its bundle fn W), visible on mobile (no hidden/sm:inline-flex), icon `h-3.5 w-3.5 mr-1.5`.
- THE BIG ONE: the live app bundle declares lucide-react v0.462.0 — its dashboard icons ship the OLD generation. Ten icons drifted geometrically (bell, log-out, mail, users, download, search, code, shopping-bag, trending-up/down); the live's MARKETING bundle ships the new generation, so the fix must scope to app contexts.
- All 0.462 icon factories extracted verbatim from the bundle; 0.525's LoaderCircle proved byte-identical (import swap only).
- The install page's Platform Instructions were WRONG: the live's WordPress path is the "Insert Headers and Footers" plugin (the clone's Theme File Editor flow was invented), Shopify rides theme.liquid via a different menu path, GTM is a Custom HTML tag flow — plus `<code>` chip literals, `font-medium` UI paths, and per-tab snippet `<pre>`s (WP/Shopify/GTM; the HTML tab has none). The GTM pre is a variant whose setAttribute takes the LITERAL site key.
- Marketing texts: the live says "company (if B2B)" (the clone had shipped B2C since the initial commit — a real visible bug) and capitalized kickers ("Perfect Fit"/"Our Process").
- 404: settled loads show the live NEVER swaps its tab title — R14-F10's evidence is disproven; the clone's NotFoundTitle swap stays as an R14-D3 value-add (docs corrected, behavior kept).
Plan written (`docs/plans/2026-09-22-round24-live-icon-generation-platform-instructions.md`) and validated against the existing test pins (the R21 export-href source pin and the R18 Save-button pin survive; the R11 kicker locators and the content-parity PlatformInstructions render sites need test-side updates).
TDD RED: 4 new files — `tests/live-icons-r24.test.tsx` (10+1 geometry pins), `tests/dashboard-r24-parity.test.tsx`, `tests/platform-instructions-r24.test.tsx`, `tests/marketing-r24-parity.test.tsx`. RED confirmed: 46/49 failing (3 pre-satisfied source pins).
GREEN: `live-icons.tsx` +10 components (extending the R16 D4 pattern); `weekOverWeekChange` in `src/lib/format.ts`; `buildPlatformSnippet` in `src/lib/snippet.ts`; `platform-instructions.tsx` rewritten (segments model: raw text / code chips / font-medium spans; per-tab pres; siteKey+collectorUrl+defaultValue props); install page passes the props; topbar (bell order, Export button, legacy icons); dashboard page (KPI icon swaps, trend badge, views label); sidebar-nav (UsersIcon on Domains); sign-out (LogOutIcon, no shrink-0); activity-feed (MailIcon); visitors-table (MailIcon/SearchIcon); settings-panel (spinner alongside label, LoaderCircle import); how-it-works (B2B + kickers); not-found comment (F12).
First GREEN run: 41/49 — the 8 failures were test-side (SSR entity encoding `&#x27;`/`&quot;`/`&lt;` in the platform assertions + a quoted-label regex in the F9 pin). Assertions corrected; the entity forms decode to the live's exact text.
Full-suite regression check: 1 failure — the R11 selection test asserted the Export href in rendered markup; updated to the R24 reality (button tag + source-level ids pin). Suite: **662 passed | 2 skipped (65 files)**.
Gates: lint 0, typecheck 0, build all routes, standalone + e2e **14/14 chromium (17.8s)**.
Browser verification on the dev server: bell/Export/Save all byte-match the settled live DOM (an early "missing base classes" observation on the live bell was a pre-hydration artifact — re-captured settled, byte-identical; the Save/Export hero-variant trimmed base is genuine and the variant-free recipe reproduces it via twMerge displacement). Platform tabs verified against the live captures. Mobile menu regression re-checked — including the same-page-link edge case, where the LIVE also keeps the Sheet open (parity, not a bug).
Screenshots (8) captured to `docs/screenshots/r24-*` and VLM-verified: the trend badge (after seeding a 3-vs-2 week state into the demo DB), the WordPress platform tab (steps + pre + chips), the B2B marketing copy, the visitors desktop/mobile Export, settings, and the mobile menu.
Docs updated: README (R24 bullet + 662), AGENTS (R24 icon/chrome/platform fact blocks + the F12-corrected 404 note), CLAUDE (mirror), PAD v1.23 (revision block, test totals, 404 correction), this session log, the repo worklog, the plan's execution log, and pixel-identifier_SKILL.md.
Final gate re-run + commit + SSH-wrapper push to main (key fingerprint SHA256:4rAzu5gC41giPSWmIojTc1isH0FGoGiSgYJkDcMp54g; dry-run then real push; remote ref verified; key shredded).
