Workspace refreshed — already at `b1581d6` (the Round-16 ship I completed earlier). Core docs re-verified (PAD v1.15, the R16 fact blocks in AGENTS/CLAUDE). Baseline gate GREEN: lint ✓, typecheck ✓, 476 tests / 51 files, build ✓ — codebase matches the documented R16 state exactly.
I had meticulously reviewed session_12 (Round-15), the round-16 plan, and session_13 (Round-16, written in the prior session) against the codebase. Round-16 was fully shipped; session_13's suggested next step was a **Round-17 drift watch** — that's this session's workstream.
Local environment rebuilt (standalone build + server with the seeded DB). The audit began with the marketing bundle: landing sections `[762,403,174,526,708,610,650,814,756,500]` + H2=7 — byte-stable. All sub-pages (docs/about/blog/terms/privacy/ccpa/gdpr) match their pins; blog slugs 10/10 identical; robots.txt format equal; favicon byte-identical. The 404s on features/pricing/faq/contact were expected — those are landing sections, not routes, on both sides.
Auth + shell: logged into the live, probed the shell — the sidebar is **byte-identical** to the R16 capture (14767 chars exact). Per-page headers aligned; the only per-token delta is the documented D4 lucide `aria-hidden`.
Then the content layer: fresh live+local captures of all 7 dashboard pages, diffed with an order-sensitive tokenizer. First a critical isolation step — **diff R16-live vs R17-live captures directly: ZERO live-side changes**. The live is 100% stable since R16.
So every remaining live-vs-local diff is clone-side. The stricter diff (exact class strings vs the R16 coarser alignment) found **3 residuals the R16 verification missed**: the activity Identified badge (wrong generation — a secondary-variant hybrid with `hover:opacity-90`, while the live ships the new-gen default-variant string byte-identical to the domains Verified badge); the pricing contact-sales card (a hand-rolled outline+asChild mailto anchor with flex-first orders vs the live's variant-free Button with the full consumer tail + twMerge-displaced card root); and three install chip wrappers (span/flex-first/aria-hidden vs the live's bare geometry-first divs). The trend chart's `role="img"`+aria-label → ruling D3: keep (invisible functional a11y).
Every other flag was investigated and classified: domains/settings buttons byte-identical (Radix + form machinery only), visitors comboboxes class-identical (aria residuals), the install notice box state-dependent (the live's site is "waiting" — the clone's waiting branch matches byte-for-byte), the live's add-domain `disabled` is CSR validation state.
Plan written and validated against the codebase (every source line confirmed), then **TDD**: 8 RED pins → GREEN — the badge rides `variant="default"` + the Verified tail; the contact card rebuilt with consumer `border-border bg-card` (twMerge reproduces the live order), p-6-first body, and a variant-free Button CTA with onClick mailto (DOM parity + functional parity); the chips rebuilt as bare geometry-first divs. One superseded shell-parity pin updated.
**Full gate GREEN: 484 tests / 51 files** (+8). Browser re-diff: the three findings eliminated; remaining flags proven tokenizer alignment noise by direct extraction. E2E green (pricing switch + $65 annual, Contact Sales, selection → Export (1), settings, install); zero console errors.
**Responsive spot-checks**: 1024px identical (chart 280px, KPI 4-col, sidebar 256px); 768px identical (280px, 2-col, 256px, table fits); 375px equivalent (both sides' pages overflow horizontally — the live's own quirk, reproduced by identical structure).
**Screenshot methodology discovery**: the live's pre-hydration shell renders DARK — my first screenshots raced it (and the clone's loading skeleton). After waiting for hydration, the loaded-page sidebar pixel-diff is **0.24%** (data-only: usage bar, avatar). The R16 0.00% comparison had raced skeletons on both sides — documented as a verification-methodology correction.
Docs updated: PAD v1.16, README (484 + R17 bullet), AGENTS.md (R17 facts), CLAUDE.md (v1.16 precedents), plan execution log, evidence README, worklog, session_14 (this log). Then ship: atomic commits on main + push via the SSH wrapper.

## What this round delivered

**Round-17 drift watch → verification-gap closure.** The audit's headline finding: **the live has been 100% stable since R16** — zero live-side class/tag changes across all 7 dashboard pages, byte-stable marketing/auth/shell. The R16 realignment target holds.

**Remediation (TDD, 8 RED → 8 GREEN pins, suite 476/51 → 484/51):**
- **Activity Identified badge**: wrong generation fixed — now the new-gen default-variant string, byte-identical to the domains Verified badge (the R16 plan had described it as new-gen but never pinned it — the gap)
- **Pricing contact-sales card**: rebuilt to the live's exact DOM — variant-free Button CTA with the full consumer tail (`transition-all` displacement signature), `onClick` mailto preserves function, card root via twMerge-displaced `border-border bg-card`, geometry-first body/subtitle orders
- **Install chips**: platform + step-number + How-It-Works chips rebuilt as bare geometry-first divs (no span wrappers, no aria-hidden)
- **Ruling D3**: trend-chart `role="img"` + aria-label kept as documented invisible-functional a11y

**Verified**: per-page structural re-diff (only documented D2/D4/aria residuals remain), E2E green with zero console errors, responsive parity at 1024/768/375px, loaded-page sidebar pixel-diff 0.24% (data-only). Methodology correction documented: the live's dark pre-hydration shell requires hydration-aware screenshot comparison. Shipped as atomic commits on main, push verified via `docs/ssh_git_wrapper_v3.py`, key shredded post-push.

**Suggested next steps**: the live is now stable across two consecutive audits — extend the drift-watch cadence (Round-18 can re-verify with the R17 tokenizer directly); consider the remaining documented residuals (aria-labels, aria-controls) for a strict-DOM closure round if byte-exact attributes become a goal; or move to deeper functional parity (e.g., the domains page's live validation states).
